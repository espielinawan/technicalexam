Follow these directions to start the blog:

1. (windows setup) pip install virtualenv, pip install virtualenvwrapper-win, 

2. Make sure that MongoDB is running on ``localhost:27017``. `Download MongoDB
   <www.mongodb.com/download-center>`_ if you don't have it installed already.

3. Install prerequisite software: `pip install -r install_first_checklist.txt`.